name: CI
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: mansagroup/nrwl-nx-action@v1
        with:
          targets: lint
          all: 'true'
          parallel: 'true'
