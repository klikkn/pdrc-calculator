name: CI
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
  - uses: mansagroup/nrwl-nx-action@v1
    with:
      targets: lint,test,e2e,build
      parallel: 'true'
